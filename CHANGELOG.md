# 变更记录

## 0.1.0-rc.0（未发布）

- 正式 ApexTable 替换 Foo，保留 TanStack v9 原生实例、上下文和状态语义。
- 新增固定行高虚拟滚动、选择轨道、分页、列设置、列宽、固定降级、展示状态和焦点处理。
- 新增独立结构/主题 CSS、语义变量、插槽 DOM 合并与列偏好入口。
- 新增服务器、本地一万/十万行、本地分页、只加载结构 CSS 的双主题、外部 atom 示例。
- 插槽按 React 组件渲染，支持 Hook/memo；补充完整控件替换、受控列偏好及存储失败示例。
- 插槽上下文采用只读属性；补充原生状态兼容性观察入口与独立的非法分页参数诊断。
- 修复偏好外部清空并重新读取后，相同布局被旧缓存跳过保存；补充显式列 ID 迁移、迁移失败及完整英文文案示例。
- 修复虚拟表格隐藏恢复时丢失滚动锚点；补充十万行容器恢复、图片失败占位与恢复示例。

迁移：Foo 已移除；用原生 useTable 创建实例并传给 ApexTable。包名改为 apex-table-react，JS与样式分别引入。正式0.1.0须在浏览器矩阵、实际呈现帧和内存验收完成后准备。
