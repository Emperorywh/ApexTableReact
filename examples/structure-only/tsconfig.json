{
  /*
   * 独立示例在核心产物生成后检查公开入口，避免源码类型检查依赖 dist。
   * 不跳过依赖声明检查，验证实际产物可供严格 TypeScript 项目消费。
   */
  "extends": "../../tsconfig.json",
  "compilerOptions": { "noEmit": true, "skipLibCheck": false },
  "include": ["App.tsx"]
}
